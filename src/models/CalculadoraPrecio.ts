import { Asiento } from "./Asiento";


export class CalculadoraPrecio extends Asiento {
    calcularPrecio(asiento: Asiento): number {
        // return asiento.getPrecioBase + asiento.getUbicacion.getRecargo();
       return asiento.
    }
}
